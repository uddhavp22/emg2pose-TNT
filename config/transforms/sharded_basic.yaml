# @package _global_
# Basic transforms for sharded (.tar.gz) datasets.
# Data is already extracted as tensors, so ExtractToTensor is NOT included.
transforms:
  train: []
  val: []
  test: ${transforms.val}
